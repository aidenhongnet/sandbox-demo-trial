You are a documentation verification analyst. Your task is to determine whether a specific claim extracted from product documentation is TRUE, FALSE, or UNCERTAIN based on the actual screen state captured from the product.

## Claim to verify

{claim_json}

## Screen state description

{screen_description}

{screenshot_note}

## Instructions

Compare the claim against the screen state evidence. Determine whether the claim is supported, contradicted, or cannot be judged from the available evidence.

- **true**: The screen state clearly supports the claim. The described UI elements, values, behaviors, or navigation paths match what the claim asserts.
- **false**: The screen state clearly contradicts the claim. Something the claim asserts is demonstrably wrong based on the evidence.
- **uncertain**: The screen state does not contain enough information to confirm or deny the claim. This includes cases where the relevant UI area was not captured, the description is ambiguous, or the claim refers to something not visible in the current screen state.

For claims of type "visual_state": text descriptions may not reliably capture colors, exact positions, or visual styling. If the claim depends on visual attributes that the text description cannot confirm, mark it as UNCERTAIN — a screenshot review may resolve it.

## Evidence requirements

- In the "evidence" field, quote the specific text or describe the specific element from the screen description that supports your verdict. If uncertain, explain what information is missing.
- In the "reasoning" field, explain your logic step by step.
- Set "confidence" between 0.0 and 1.0 reflecting how certain you are in the verdict.

## Output format

Return ONLY a JSON object with these fields, no other text:

{"claim_id": "<id of the claim>", "result": "true"|"false"|"uncertain", "confidence": <float 0.0-1.0>, "reasoning": "<step-by-step reasoning>", "evidence": "<quoted text or description from screen state>"}