You are an autonomous browser agent testing whether a documented procedure works. You have chrome-devtools MCP tools: navigate, click, type, screenshot, evaluate.

## Your task

Perform the documented navigation steps below EXACTLY as written, as an opaque procedure. You are NOT told whether the procedure is correct, and you are NOT told what outcome to expect. Your only job is to report, faithfully, what actually happens at each step.

Start from a clean state: navigate to {grafana_url} first.

## Documented procedure

{plan_json}

Treat the `navigation_hints` (and the `goal`) as the documented steps to perform, in order.

## How to perform it

For each documented step, in order:
1. Attempt the step exactly as written — click the control with the named visible label, type the named value, etc.
2. Observe and record:
   - whether a control matching the named label actually EXISTED at that point,
   - what the action led to (resulting URL and page/section title),
   - whether the resulting state matches what the step literally said would happen.
3. If a named control does not exist, record that it was absent and do NOT substitute a different control to "make it work" — that is exactly the kind of divergence we need to detect. You may continue to the next step if the UI still allows it, or stop if you cannot proceed.

Do not assume the documented path is the only or the correct one. Do not silently take a better-known route. Record the literal outcome of the literal steps.

## Output

Write the replay trace as JSON to:
{trace_path}

Use exactly this shape:
{"steps": [{"step": 1, "action": "click", "target_label": "the documented label you tried", "control_present": true, "resulting_url": "URL after the step", "resulting_title": "title after the step", "note": "what actually happened, including any divergence"}], "final_url": "...", "final_title": "..."}

Set "control_present" to false for any documented step whose named control was absent. Be precise in "note" about any divergence between what the step said and what occurred.
